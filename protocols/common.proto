syntax = "proto3";

package com.pb;

enum ROOM_STATE {
    ROOM_STATE_ALL = 0;
    ROOM_STATE_EMPTY = 1;
    ROOM_STATE_OCCUPY = 2;
}

message CommonResponse {
    int32 code = 1; // return 0 if success
    string error = 2;
}