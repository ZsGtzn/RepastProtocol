syntax = "proto3";

package com.pb;

enum AUTHORITY
{
    AUTHORITY_BOOK_ROOM = 0; // 预定房间（包括预定，退订，扫台）
    AUTHORITY_ORDER_DISH = 1; // 点菜（包括点菜、加菜、退菜）
    AUTHORITY_SETTLE_ACCOUNT = 2; // 结算
    AUTHORITY_SETTLE_ACCOUNT_DISCOUNT = 3; // 结算时候的折扣
}